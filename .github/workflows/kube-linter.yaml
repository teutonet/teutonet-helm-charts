name: Lint Yaml

on:
  pull_request:
    paths:
      - charts/**
  merge_group:
    types:
      - checks_requested

jobs:
  scanYaml:
    name: Scan Yaml
    runs-on: ubuntu-latest
    steps:
      - uses: stackrox/kube-linter-action@v1
        with:
          directory: charts
