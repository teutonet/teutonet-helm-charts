worker:
  database:
    host: localhost
    credentials:
      secret:
        name: db-credentials
  config:
    flavourPrefixFilter:
      - teuto
      - teuto-net
    syncInterval: 10s
    loggingFormat: json
    environment: production
  kubeconfig:
    secret:
      name: k8s-access-token
