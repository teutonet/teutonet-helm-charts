apiVersion: v1
kind: Service
metadata: {{- include "chirpstack.commonMetadata" (dict "context" . "component" "rest-api") | nindent 2 }}
spec:
  ports:
    - name: rest-api
      port: 8090
      targetPort: rest-api
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: rest-api
