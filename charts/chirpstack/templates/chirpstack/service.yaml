apiVersion: v1
kind: Service
metadata: {{- include "chirpstack.commonMetadata" (dict "context" . "component" "chirpstack") | nindent 2 }}
spec:
  ports:
    - name: "chirpstack"
      port: 8080
      targetPort: chirpstack
  selector: {{- include "common.labels.matchLabels" . | nindent 4 }}
    app.kubernetes.io/component: chirpstack
