# yaml-language-server: $schema=https://raw.githubusercontent.com/teutonet/teutonet-helm-charts/main/charts/base-cluster/values.schema.json

global:
  clusterName: artifacthub
  serviceLevelAgreement: None
flux:
  gitRepositories:
    main:
      url: ssh://user@example.com/repo.git
      decryption:
        provider: sops
kyverno:
  enabled: true
storage:
  readWriteMany:
    enabled: true
monitoring:
  grafana:
    adminPassword: default
