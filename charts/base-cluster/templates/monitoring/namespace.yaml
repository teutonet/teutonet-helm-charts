{{- if or .Values.monitoring.prometheus.enabled .Values.monitoring.metricsServer.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels: {{- include "common.labels.standard" $ | nindent 4 }}
    app.kubernetes.io/component: monitoring
{{- end }}