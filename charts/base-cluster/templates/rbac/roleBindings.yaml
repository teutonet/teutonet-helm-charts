{{- $roles := include "base-cluster.rbac.roles" (dict "accounts" .Values.rbac.accounts "roles" (.Values.rbac.roles | keys)) | fromYaml -}}

{{- range $roleName, $roleMapping := $roles -}}
  {{- $clusterMapping := dig "clusterMapping" (dict) $roleMapping -}}
  {{- $namespaceMapping := dig "namespaceMapping" (dict) $roleMapping -}}
  {{- range $namespace, $accounts := $namespaceMapping }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ printf "%s-%s" (include "common.names.fullname" $) $roleName }}
  labels: {{- include "common.labels.standard" $ | nindent 4 }}
    app.kubernetes.io/component: rbac
  namespace: {{ $namespace }}
subjects:
    {{- range $account := $accounts }}
  - kind: ServiceAccount
    namespace: {{ $.Release.Namespace }}
    name: {{ $account }}
    {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ $roleName }}
  {{- end }}
  {{- if $clusterMapping }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ printf "%s-%s" (include "common.names.fullname" $) $roleName }}
  labels: {{- include "common.labels.standard" $ | nindent 4 }}
    app.kubernetes.io/component: rbac
subjects:
    {{- range $account := $clusterMapping }}
  - kind: ServiceAccount
    namespace: {{ $.Release.Namespace }}
    name: {{ $account }}
    {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ $roleName }}
  {{- end }}
{{- end }}